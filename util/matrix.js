export const MATRIX_OPTIONS = [
  'APPLY_CHARACTERS_WHILE_SCORING',
  // 0 = disable scoring
  // 1 = enable scoring

  'ALLOW_OVERWRITING_BY_RULES',
  // 0 = disallow overwriting existing scores
  // 1 = allow overwriting existing scores

  'DISABLE_SCORING',
  // 0 = scoring is enabled
  // 1 = scoring is disabled

  'ENABLE_CELL_MEDIA_AUTOMATION',
  // 0 = cell media automation is disabled
  // 1 = cell media automation is enabled

  'DEFAULT_NUMBERING_MODE',
  // 0 NEXUS
  // 1 TNT

  'DEFAULT_MULTISTATE_TAXA_MODE',
  // 0 = Polymorphic
  // 1 = Uncertain
]
